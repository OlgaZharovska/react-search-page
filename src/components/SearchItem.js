import React from "react";

export const SearchItem = ({ content }) => (
  <div className="phrasebox">
    <div className="card-body">
      <p className="card-text">{content}</p>
    </div>
  </div>
);
